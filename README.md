# Meal Planner
A simple home page that shows all the available meals in each day.



The database is not included:
For creating database, run the following code in an active virtual environment.
```python
python manage.py migrate
```